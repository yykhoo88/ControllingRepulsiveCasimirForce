function y = lol(x,times,testlength)
y=x;
for i=1:times
    y=x.^y;
end
end



%{
%numerical approximation to x^x^x....=2
function y = lol(testlength)
%y=zeros(1,testlength);
%for i=1:testlength
    y=fzero(@(x) x.^x-2,0.1);
    %sprintf('For Testlength=%d, answer =%e',i,y(i));
%end
end

function result = infp(x,times)
    
    result=x.^x .^x- 2;
end




    a=x;
    for i=1:times
        a=a.^x;
        sprintf('%d\n',i);
    end

%}